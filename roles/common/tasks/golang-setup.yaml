- name: Install golang
  ansible.builtin.dnf:
    name: golang
    state: present 

- name: Install the Python packages 
  ansible.builtin.package:
    name: "{{item}}"
    state: present 
  loop:
    - go mod init dispatch
    - go get
    - go build
  args:
    chdir: /app

 
